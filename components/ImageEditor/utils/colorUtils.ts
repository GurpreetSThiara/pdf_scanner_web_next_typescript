export function rgbToHsl(r: number, g: number, b: number): [number, number, number] {
  // ... existing rgbToHsl code ...
}

export function hslToRgb(h: number, s: number, l: number): [number, number, number] {
  // ... existing hslToRgb code ...
} 